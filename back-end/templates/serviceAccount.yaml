apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    {{- include "back-end.labels" . | nindent 4 }}
  name: {{ .Release.Name }}-{{ template "back-end.serviceAccountName" . }}
  namespace: {{ .Values.namespace.name }}